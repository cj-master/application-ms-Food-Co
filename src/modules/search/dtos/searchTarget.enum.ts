// ─── Tipos de resultado ───────────────────────────────────────────────────────

export enum SearchTargetEnum {
  ALL         = 'all',          // busca en todo (default)
  POSTS       = 'posts',
  USERS       = 'users',
  RESTAURANTS = 'restaurants',
}